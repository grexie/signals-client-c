#include "grexie_signals_client.h"

#include <stdio.h>

int main(void) {
    printf("signalsbot C example: wire gsc_client_t send/recv callbacks, initialize gsc_signals_manager_t, and handle GSC_EVENT_CREATE_MARKET_ORDER events from the websocket.\n");
    return 0;
}
